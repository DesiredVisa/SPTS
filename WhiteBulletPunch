while true do
task.wait(0)
for _, bullet in pairs(game.Workspace:GetChildren()) do
    if bullet.Name == "Part" then
       bullet.Transparency = 0
       bullet.Color = Color3.fromRGB(255, 255, 255)
    end
    end
end
